import { TestBed } from '@angular/core/testing';
import { GoogleDisplayService } from './google-display.service';
describe('GoogleDisplayService', () => {
    beforeEach(() => TestBed.configureTestingModule({}));
    it('should be created', () => {
        const service = TestBed.get(GoogleDisplayService);
        expect(service).toBeTruthy();
    });
});
//# sourceMappingURL=google-display.service.spec.js.map