import { TestBed } from '@angular/core/testing';
import { GoogleResponsiveDisplayService } from './google-responsive-display.service';
describe('GoogleResponsiveDisplayService', () => {
    beforeEach(() => TestBed.configureTestingModule({}));
    it('should be created', () => {
        const service = TestBed.get(GoogleResponsiveDisplayService);
        expect(service).toBeTruthy();
    });
});
//# sourceMappingURL=google-responsive-display.service.spec.js.map